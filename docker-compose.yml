version: '3.8'

services:
  app:
    build: .
    container_name: smr-zn-prk-app
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - STREAMLIT_SERVER_PORT=8501
    # Configuración opcional para GPU (descomentar si se usa NVIDIA CUDA en Docker)
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
